//Header might not be necessary for a program this small, but it helped me to separate functions to make the Catch2 testing work better.
#include <cmath>
#include <iostream>

double gridpi(unsigned npoints);